<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <% devs.forEach(dev => { %>
    <a href="/devs/<%= dev._id %>"><%= dev.name %> </a>
    <% }) %>
    <!-- devs.ForEach list out each dev && icon -->
</body>
</html>